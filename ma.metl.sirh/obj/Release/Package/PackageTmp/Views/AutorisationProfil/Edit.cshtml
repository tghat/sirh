@model ma.metl.sirh.Model.AutorisationProfil

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>AutorisationProfil</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.Autorisation_Id, "Autorisation_Id", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Autorisation_Id", String.Empty)
                @Html.ValidationMessageFor(model => model.Autorisation_Id)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Profil_Id, "Profil_Id", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Profil_Id", String.Empty)
                @Html.ValidationMessageFor(model => model.Profil_Id)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
